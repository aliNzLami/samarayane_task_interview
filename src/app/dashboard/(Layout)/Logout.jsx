'use client'

function Logout() {

    const handleLogout = () => {
        
    }

    return (
        <div onClick={handleLogout} className='sidebarItems text-20 mb-4 cursor-pointer'>
            خروج
        </div>
    )
}

export default Logout